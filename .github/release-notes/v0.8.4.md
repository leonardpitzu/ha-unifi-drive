# Home Assistant integration for UniFi Drive / UNAS v0.8.4 - Throughput fallback and compatibility validation

Patch release focused on live throughput compatibility and validation reliability
for the `0.8.x` line.

## Breaking change reminder: reinstall required from versions before 0.8.0

Versions before `0.8.0` should not be upgraded in place. Remove the old Home
Assistant integration entry first, then remove the old custom integration
installation before installing this release again. This is required so the old
`custom_components/unifi_drive` folder is removed and Home Assistant creates a
fresh `unifi_unas` config entry and entity registry state.

Recommended upgrade path:

1. In Home Assistant, open `Settings` -> `Devices & services`.
2. Open the existing UniFi Drive / UNAS integration entry.
3. Choose `Delete` and confirm removal of the config entry. This removes the
   old configured device from Home Assistant.
4. Restart Home Assistant.
5. In HACS, remove the old custom integration installation. A normal in-place
   update is not enough when the old integration folder still exists.
6. Restart Home Assistant again so the old custom integration folder is no
   longer loaded.
7. Install this release through HACS again. For manual installs, delete
   `custom_components/unifi_drive/` and copy the current
   `custom_components/unifi_unas/` folder.
8. Restart Home Assistant after the new files are in place.
9. Add `UniFi Drive / UNAS` from `Settings` -> `Devices & services`.
10. Configure the device again and re-enable only the optional features you want
    to expose.

Do not copy an old config entry back into Home Assistant storage. Recreate the
entry through the UI so discovery identity, unique IDs, diagnostics and optional
feature state are initialized with the current release behavior.

## Highlights

- Adds a read-only network I/O fallback for live throughput sensors when the
  storage payload reports zero values during active SMB traffic.
- Maps NAS transmit traffic to Home Assistant read throughput and NAS receive
  traffic to Home Assistant write throughput.
- Keeps storage-payload throughput preferred when it reports non-zero values.
- Preserves zero as a valid idle state when no source reports traffic.
- Adds privacy-safe throughput diagnostics with selected source and
  zero/non-zero/missing status only.
- Adds a validation matrix for Home Assistant `2024.8.0` on Python `3.12` and
  Home Assistant `2026.6.0` on Python `3.14`.
- Pins the matching `pytest-homeassistant-custom-component` release for each
  Home Assistant test line.
- Installs the Home Assistant test plugin without its pinned Home Assistant
  dependency so the matrix runtime remains the source of truth.
- Uses Home Assistant package constraints during CI dependency installation.
- Keeps `mypy` targeting Python `3.12` while HACS still advertises Home
  Assistant `2024.8.0` compatibility.
- Keeps release preflight jobs on Python `3.14`.
- Removes a pytest option that is not recognized by the older supported
  pytest-asyncio line.

## Validation

- Full pytest suite: `504 passed`
- `python scripts/check_repo.py`
- `ruff check custom_components tests scripts`
- `mypy --config-file mypy.ini`
- `python -m compileall -q custom_components/unifi_unas tests scripts`
- `git diff --check`
