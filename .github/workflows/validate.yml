name: Validate

on:
  push:
    branches:
      - main
      - "v*"
      - chore/**
      - feature/**
  pull_request:
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hacs:
    name: HACS
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: HACS validation
        uses: hacs/action@main
        with:
          category: integration

  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Hassfest validation
        uses: home-assistant/actions/hassfest@master

  repository_checks:
    name: Repository checks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - homeassistant: "2024.8.0"
            python: "3.12"
            pytest_homeassistant: "0.13.152"
          - homeassistant: "2026.6.0"
            python: "3.14"
            pytest_homeassistant: "0.13.334"
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
          check-latest: true

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install \
            -c "https://raw.githubusercontent.com/home-assistant/core/${{ matrix.homeassistant }}/homeassistant/package_constraints.txt" \
            "homeassistant==${{ matrix.homeassistant }}" \
            pytest \
            aiohttp \
            "josepy<2.0; python_version < '3.13'" \
            ruff \
            bandit \
            coverage \
            mypy \
            packaging
          pip install --no-deps \
            "pytest-homeassistant-custom-component==${{ matrix.pytest_homeassistant }}"
          python - <<'PY' > /tmp/pytest-homeassistant-custom-component-requirements.txt
          from importlib.metadata import requires
          from packaging.requirements import Requirement

          for requirement in requires("pytest-homeassistant-custom-component") or []:
              if Requirement(requirement).name == "homeassistant":
                  continue
              print(requirement)
          PY
          pip install \
            -c "https://raw.githubusercontent.com/home-assistant/core/${{ matrix.homeassistant }}/homeassistant/package_constraints.txt" \
            -r /tmp/pytest-homeassistant-custom-component-requirements.txt

      - name: Verify Home Assistant test version
        run: |
          python - <<'PY'
          import os
          import importlib.metadata as md

          expected = os.environ["EXPECTED_HOMEASSISTANT_VERSION"]
          version = md.version("homeassistant")
          if version != expected:
              raise SystemExit(f"Expected Home Assistant {expected}, got {version}")
          print(f"Home Assistant test version: {version}")
          PY
        env:
          EXPECTED_HOMEASSISTANT_VERSION: ${{ matrix.homeassistant }}

      - name: Run repository checks
        env:
          UNIFI_UNAS_FORBIDDEN_MARKERS: ${{ vars.UNIFI_UNAS_FORBIDDEN_MARKERS }}
        run: python scripts/check_repo.py

      - name: Run lint checks
        run: ruff check custom_components tests scripts

      - name: Run typing checks
        run: mypy --config-file mypy.ini

      - name: Run security checks
        run: bandit -q -r custom_components scripts

      - name: Run complete test suite
        run: python -m coverage run -m pytest -q

      - name: Enforce coverage floor
        run: python -m coverage report

      - name: Compile integration and tests
        run: python -m compileall -q custom_components/unifi_unas tests

      - name: Check diff whitespace
        run: git diff --check
